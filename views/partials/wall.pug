mixin wall(wall_posts)
	div.r_flex.center.wall
		each p in wall_posts
			a(href=`/post/album/${p.publicacion_id}/${p.id}`)
				div.post
					img(src=`${p.img_b64}`, width=300, height=600)
					h3 #{p.titulo}